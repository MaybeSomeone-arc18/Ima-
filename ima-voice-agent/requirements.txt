# Moss semantic search SDK (>=1.9.0, per docs.moss.dev/docs/integrations/livekit)
moss>=1.9.0

# LiveKit Agents framework + the plugins this agent uses: Deepgram (STT),
# OpenAI (TTS, plus LLM fallback if GOOGLE_API_KEY isn't set), Silero (VAD),
# the turn-detector model, and the Gemini LLM plugin (preferred for the LLM).
livekit-agents[deepgram,openai,silero,turn-detector,google]~=1.8

python-dotenv
httpx
